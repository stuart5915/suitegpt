{
  "name": "basis-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:vault": "hardhat run scripts/deploy-basis-vault.js --network base",
    "deploy:vault:testnet": "hardhat run scripts/deploy-basis-vault.js --network baseSepolia",
    "deploy:staking": "hardhat run scripts/deploy-basis-staking.js --network base",
    "deploy:staking:testnet": "hardhat run scripts/deploy-basis-staking.js --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "@openzeppelin/contracts-upgradeable": "^5.4.0",
    "@openzeppelin/hardhat-upgrades": "^3.9.1",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "ethers": "^6.16.0"
  }
}
